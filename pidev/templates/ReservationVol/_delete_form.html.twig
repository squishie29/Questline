<form method="post" action="{{ path('ReservationVol_delete', {'id_res': ReservationVols.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ReservationVols.id) }}">
  <center>  <button type="submit" class="btn btn-primary mt-3" class="btn" class="btn">Delete</button>
</center>
</form>
