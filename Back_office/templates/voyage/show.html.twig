{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}

    <h1>Voyage</h1>

    <table class="table" border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ voyage.Destination }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voyage.Prix }}</td>
            </tr>
            <tr>
                <th>Periode</th>
                <td>{{ voyage.Periode }}</td>
            </tr>
        </tbody>
    </table>
    <center>
    <a href="{{ path('voyage_index') }}"><button type="submit" class="btn btn-primary mt-3" class="btn" class="btn">back to list</button></a>
    <br>

    <a href="{{ path('voyage_edit', {'id': voyage.id}) }}"><button type="submit" class="btn btn-primary mt-3" class="btn" class="btn">edit</button></a>

    {{ include('voyage/_delete_form.html.twig') }}
    </center>
{% endblock %}
